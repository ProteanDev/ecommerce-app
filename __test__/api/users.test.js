// import { createMocks } from 'node-mocks-http'
// import usersApi from '../../pages/api/users'

describe('Test Users', () => {
  it('Route works', async () => {
    // const { req, res } = createMocks({
    //   method: 'GET',
    //   params: {},
    // })

    // await usersApi(req, res)

    // expect(res._getStatusCode()).toBe(200)
    expect(true).toBeTruthy()
  })
})
